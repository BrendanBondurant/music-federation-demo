# Ensembles are artists too: type ENSEMBLE, instrument "ensemble".
# Same @key join as a person.
{
  artist(id: "modern-jazz-quartet") {
    name
    type
    instrument
    albums {
      title
      year
      label
    }
  }
}
